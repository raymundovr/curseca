import { Box } from '@mui/material';
import React from 'react';

const Catalogue = () => {
    return (
        <Box>
            <h1>Catálogo</h1>
        </Box>
    );
};

export default Catalogue;
